$(function() {
  $('#sendMsgInput').on('keydown', function(event) {
    if (event.keyCode == 13) {
      if (!event.shiftKey) {
        $('#sendMsgForm').submit();
      }
    }
  });

  location.hash = "#last-msg";
});
